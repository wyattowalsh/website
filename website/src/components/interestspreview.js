/* Interests component for landing page of my personal website (Gatsby.js) */

import * as React from "react"
import * as styles from "./interestspreview.module.scss"
import { graphql } from 'gatsby'
import { Grid, Container, Typography } from '@mui/material';

const InterestsPreview = ({ data }) => {
    return (
        null
    )
}

export default InterestsPreview;

// export const InterestsPreviewQuery = graphql`
//   query InterestsPreviewQuery {
//     mdx(slug: {eq: $slug}) {
//         slug
//     }
//   }
// `